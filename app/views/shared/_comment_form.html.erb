<%= form_for(@comment) do |f| %>
    <div class="field">
      <%= f.text_area :info, placeholder: "Compose new comment..." %>
      <input type="number" name="book_id" value=<%= @book.id %> readonly>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>